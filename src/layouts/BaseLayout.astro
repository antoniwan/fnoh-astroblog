---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import ScrollToTop from "../components/ui/ScrollToTop.astro";

interface Props {
  title: string;
  description: string;
  image?: string;
  showHeader?: boolean;
  showFooter?: boolean;
  class?: string;
  type?: "website" | "article" | "organization";
  publishedTime?: string;
  modifiedTime?: string;
  author?: string;
  section?: string;
  tags?: string[];
}

const {
  title,
  description,
  image,
  showHeader = true,
  showFooter = true,
  class: className = "",
  type = "website",
  publishedTime,
  modifiedTime,
  author,
  section,
  tags = [],
} = Astro.props;
---

<!doctype html>
<html lang="en" class="h-full">
  <head>
    <BaseHead
      title={title}
      description={description}
      image={image}
      type={type}
      publishedTime={publishedTime}
      modifiedTime={modifiedTime}
      author={author}
      section={section}
      tags={tags}
    />
  </head>
  <body
    class={`${className} min-h-full bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 antialiased`}
  >
    {showHeader && <Header />}

    <main class="pt-16 min-h-screen">
      <slot />
    </main>

    {showFooter && <Footer />}

    <ScrollToTop position="bottom-right" threshold={300} />
  </body>
</html>
