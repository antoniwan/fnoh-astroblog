---
import BaseLayout from '../layouts/BaseLayout.astro';

import { SITE_TITLE, SITE_DESCRIPTION, CATEGORIES } from '../consts';
---

<BaseLayout 
	title={`About - ${SITE_TITLE}`} 
	description={SITE_DESCRIPTION}
	type="website"
	section="About"
>
	<div class="about-container container-md">
		<div class="about-content">
			<h1>About the Field Notes on Humanity</h1>
			
			<p>
				I've always wanted to "write this book". My field journal. My personalized record of this 'modern' experiement we call life. Not a theory of humanity, not a doctrine but what I've observed and am able to report. My understandings of what it means to be human.
			</p>
			<p>
				I write these notes so I don't forget what it costs, and what it gives, to live.
			</p>
			<p>
				My field notes are organized into several areas of inquiry, each representing a different lens through which I observe and document my human experience.
			</p>
			
			<ul class="categories-list">
				{Object.entries(CATEGORIES).map(([key, category]) => (
					<li>
						<strong>{category.emoji} {category.title}</strong> — {category.description}
						<a href={`/${key}`} class="category-link">Explore →</a>
					</li>
				))}
			</ul>
			<p class="closing">
				These are my coordinates. Draw your own map.
			</p>
		</div>
	</div>
</BaseLayout>

<style>
	.about-container {
		min-height: 60vh;
		padding: var(--spacing-xl) 0;
	}

	.about-content {
		max-width: 700px;
		margin: 0 auto;
	}

	.about-content h1 {
		font-size: var(--font-size-4xl);
		margin-bottom: var(--spacing-lg);
		color: var(--color-text);
		text-align: center;
	}

	.about-content h2 {
		font-size: var(--font-size-2xl);
		margin: var(--spacing-xl) 0 var(--spacing-md) 0;
		color: var(--color-text);
		border-bottom: 2px solid var(--color-border);
		padding-bottom: var(--spacing-sm);
	}

	.about-content .intro {
		font-size: var(--font-size-xl);
		color: var(--color-text);
		line-height: var(--line-height-relaxed);
		font-weight: 500;
		margin-bottom: var(--spacing-lg);
		text-align: center;
		font-style: italic;
	}

	.about-content p {
		font-size: var(--font-size-lg);
		color: var(--color-text-light);
		line-height: var(--line-height-relaxed);
		margin-bottom: var(--spacing-md);
	}

	.about-content strong {
		color: var(--color-text);
	}

	.about-content ul {
		margin: var(--spacing-md) 0 var(--spacing-lg) var(--spacing-lg);
		padding-left: var(--spacing-md);
	}

	.about-content li {
		font-size: var(--font-size-lg);
		color: var(--color-text-light);
		line-height: var(--line-height-relaxed);
		margin-bottom: var(--spacing-sm);
	}

	.categories-list {
		margin: var(--spacing-lg) 0 var(--spacing-xl) var(--spacing-lg);
		padding-left: var(--spacing-md);
	}

	.categories-list li {
		font-size: var(--font-size-lg);
		color: var(--color-text-light);
		line-height: var(--line-height-relaxed);
		margin-bottom: var(--spacing-md);
	}

	.category-link {
		display: inline-block;
		color: var(--color-primary);
		text-decoration: none;
		font-weight: var(--font-weight-medium);
		font-size: var(--font-size-sm);
		text-transform: uppercase;
		letter-spacing: 0.5px;
		margin-left: var(--spacing-sm);
		transition: color 0.2s ease;
	}

	.category-link:hover {
		color: var(--color-primary-dark);
		text-decoration: underline;
	}

	.about-content .closing {
		font-size: var(--font-size-xl);
		color: var(--color-text);
		font-weight: 500;
		text-align: center;
		font-style: italic;
		margin-top: var(--spacing-xl);
	}

	@media (min-width: 768px) {
		.categories-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}
</style>