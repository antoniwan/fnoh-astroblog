---
import BaseLayout from "../layouts/BaseLayout.astro";

import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import { getAllCategories } from "../utils/categories";

// Get all categories
const categories = await getAllCategories();
---

<BaseLayout
  title={`About - ${SITE_TITLE}`}
  description={SITE_DESCRIPTION}
  type="website"
  section="About"
>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="max-w-3xl mx-auto">
      <h1 class="text-4xl font-bold text-gray-900 mb-8 text-center">About the Field Notes on Humanity</h1>

      <div class="space-y-6 text-lg text-gray-700 leading-relaxed">
        <p>
          I've always wanted to "write this book". My field journal. My
          personalized record of this 'modern' experiement we call life. Not a
          theory of humanity, not a doctrine but what I've observed and am able to
          report. My understandings of what it means to be human.
        </p>
        <p>
          I write these notes so I don't forget what it costs, and what it gives,
          to live.
        </p>
        <p>
          My field notes are organized into several areas of inquiry, each
          representing a different lens through which I observe and document my
          human experience.
        </p>

        <ul class="space-y-4 mt-8">
          {
            categories.map((category) => (
              <li class="bg-gray-50 p-4 rounded-lg border border-gray-200">
                <div class="flex items-start justify-between">
                  <div class="flex-1">
                    <strong class="text-gray-900">
                      {category.emoji} {category.title}
                    </strong>
                    <span class="text-gray-600"> — {category.description}</span>
                  </div>
                  <a href={`/${category.slug}`} class="text-blue-600 hover:text-blue-800 transition-colors duration-200 font-medium ml-4">
                    Explore →
                  </a>
                </div>
              </li>
            ))
          }
        </ul>
        <p class="text-center text-xl font-medium text-gray-800 mt-12 italic">These are my coordinates. Draw your own map.</p>
      </div>
    </div>
  </div>
</BaseLayout>
